<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- BLE advertising + scanning (Android 12+, Karoo 3) -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <!-- Legacy Bluetooth permissions for API <= 30 (Karoo 2) -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Own GPS position (LocationManager GPS provider: Location.getTime() is satellite UTC) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Android 11+: without it, a location foreground service started from the background (boot,
         Karoo OS binding the extension) receives no fixes at all. See requiredPermissions(). -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Start the link service after boot without waiting for Karoo OS to bind the extension -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <!-- No MANIFEST_URL while this repository is private: the update-discovery URL has to be
             fetchable without credentials, and GitHub serves 404 for a private repo's release
             assets to anyone unauthenticated. Karoo OS and the phone companion app have no GitHub
             session (only a browser does), so pointing at one turned every install into a
             "download failed". See CLAUDE.md "Update discovery" before adding it back. -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".extension.PartnerRideExtension"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="io.hammerhead.karooext.KAROO_EXTENSION" />
            </intent-filter>
            <meta-data
                android:name="io.hammerhead.karooext.EXTENSION_INFO"
                android:resource="@xml/extension_info" />
        </service>

        <service
            android:name=".service.PartnerLinkService"
            android:exported="false"
            android:foregroundServiceType="location|connectedDevice"
            tools:targetApi="q" />

        <!-- Tap on the ride data field. Unexported: the field's PendingIntent is created by
             this app, so the broadcast is dispatched under our own identity even though Karoo's
             process is what fires it. -->
        <receiver
            android:name=".service.FieldTapReceiver"
            android:exported="false" />

        <!-- BOOT_COMPLETED is a protected broadcast: only the system can send it. -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
